<%- include('partials/header'); -%>

    <div class="container p-4">
        <div class="row">
            <div class="col-md-5">
                <div class="card">
                    <div class="card-body">
                        <form action="/addclient" method="POST">
                            <div class="input-group mb-3">
                                <span class="input-group-text">Cliente: </span>
                                <input type="text" class="form-control" placeholder="Apellido, Nombre" name="name_client">
                            </div>
                            <div class="input-group mb-3">
                                <span class="input-group-text">Contenedor Id: </span>
                                <input type="text" class="form-control" placeholder="00" name="id_container">
                            </div>
                            <div class="input-group mb-3">
                                <span class="input-group-text">Valor Mensual $</span>
                                <input type="text" class="form-control" placeholder="5000" name="price_tocharge">
                            </div>
                            <button class="btn btn-block btn-outline-primary" type="submit"
                                id="button-addon2">ACEPTAR</button>

                        </form>
                    </div>
                </div>
            </div>
            <!-- DATABASE TABLE -->
            <div class="col-md-7">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Cliente</th>
                            <th>Precio-Cobrar</th>
                            <th>Activo</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% clientes.forEach(function (cl) { %>
                            <tr>
                                <td><%= cl.id_container %></td>
                                <td><%= cl.name_client %></td>
                                <td><%= format(cl.created_at) %></td>
                                <td>
                                    <a href="/delete/<%= tl._id %>" class="btn btn-outline-danger">Delete</a>
                                </td>
                            </tr>
                            <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <%- include('partials/footer'); -%>